language: c
 
install:
  - sudo apt-get install mono-devel mono-gmcs nunit-console --fix-missing
script:
  - cd RestClientCs && xbuild RestClinetCs.csproj
  - cd RestClientCs && nunit-console ./tests/FluentMigrator.Tests/bin/Debug/FluentMigra
